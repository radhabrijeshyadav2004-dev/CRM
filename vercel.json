// {
// "version": 2,
// "rewrites": [
// { "source": "/api/(.*)", "destination": "server.js" },
// { "source": "/(.*)", "destination": "server.js" }
// ]
// }

{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    }
  ]
}
